# coding: utf-8

"""
STACKIT PubSub API

This API provides endpoints for managing PubSubs.

The version of the OpenAPI document: 1beta.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    StrictBool,
    StrictStr,
    field_validator,
)
from pydantic_core import to_jsonable_python
from typing_extensions import Annotated, Self


class TopicSettings(BaseModel):
    """
    Configuration settings for the topic.
    """  # noqa: E501

    acl: Optional[Annotated[List[StrictStr], Field(max_length=100)]] = Field(
        default=None,
        description="Whitelist of IPs in CIDR notation that can access the topic and its underlying subscriptions.",
    )
    metrics: Optional[StrictBool] = Field(
        default=False, description="Enables or disables metrics collection for the topic."
    )
    retention: Optional[Annotated[str, Field(strict=True)]] = Field(
        default="168h",
        description="The maximum length of time a message is persisted in storage.  Messages older than this duration are automatically discarded/deleted.  **Format:** Must be a scalar duration: a number (integer or decimal) followed by a unit ('s', 'm', or 'h').  **Constraints:** - **Minimum:** \"1s\" - **Maximum:** \"168h\" (7 days) ",
    )
    service_logs: Optional[StrictBool] = Field(
        default=False, description="Enables or disables sending of service logs for the topic.", alias="serviceLogs"
    )
    __properties: ClassVar[List[str]] = ["acl", "metrics", "retention", "serviceLogs"]

    @field_validator("retention")
    def retention_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9]+(\.[0-9]+)?(s|m|h)$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+(\.[0-9]+)?(s|m|h)$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TopicSettings from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TopicSettings from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "acl": obj.get("acl"),
                "metrics": obj.get("metrics") if obj.get("metrics") is not None else False,
                "retention": obj.get("retention") if obj.get("retention") is not None else "168h",
                "serviceLogs": obj.get("serviceLogs") if obj.get("serviceLogs") is not None else False,
            }
        )
        return _obj
