# coding: utf-8

"""
STACKIT PubSub API

This API provides endpoints for managing PubSubs.

The version of the OpenAPI document: 1beta.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import BaseModel, ConfigDict, Field, field_validator
from pydantic_core import to_jsonable_python
from typing_extensions import Annotated, Self

from stackit.pubsub.models.subscription_settings import SubscriptionSettings


class CreateSubscriptionPayload(BaseModel):
    """
    CreateSubscriptionPayload
    """  # noqa: E501

    description: Optional[Annotated[str, Field(strict=True, max_length=1024)]] = Field(
        default=None,
        description="The description is a longer text chosen by the user to provide more context for the resource.",
    )
    display_name: Annotated[str, Field(min_length=1, strict=True, max_length=36)] = Field(
        description="The display name is a short name chosen by the user to identify the resource.", alias="displayName"
    )
    labels: Optional[Dict[str, Optional[Annotated[str, Field(strict=True, max_length=63)]]]] = Field(
        default=None,
        description="Labels are key-value pairs used to group and organize subsets of resources.  **Key constraints:** - May contain an optional domain prefix separated by a slash (`/`). - The domain prefix must be less than or equal to 250 characters and a valid DNS subdomain containing only lowercase alphanumerics (`[a-z0-9]`) and dashes (`-`), separated by dots (`.`). - The name part (excluding the domain prefix) must be between 1 and 63 characters long. - The name part must begin and end with an alphanumeric character (`[a-z0-9A-Z]`). - The name part may contain dashes (`-`), underscores (`_`), dots (`.`), and alphanumerics in between. - Keys starting with the prefix `stackit-` or having a domain prefix of `stackit.cloud` (including its subdomains, e.g., `*.stackit.cloud/`) are reserved for system use.  **Value constraints:** - Must be less than or equal to 63 characters long (can be empty). - Must begin and end with an alphanumeric character (`[a-z0-9A-Z]`) if not empty. - May contain dashes (`-`), underscores (`_`), dots (`.`), and alphanumerics in between.  **Count constraint:** - A maximum of 64 labels can be applied per resource. Restricted labels are excluded from this count. ",
    )
    settings: Optional[SubscriptionSettings] = None
    __properties: ClassVar[List[str]] = ["description", "displayName", "labels", "settings"]

    @field_validator("display_name")
    def display_name_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[a-zA-Z0-9 -]*$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z0-9 -]*$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreateSubscriptionPayload from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of settings
        if self.settings:
            _dict["settings"] = self.settings.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreateSubscriptionPayload from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "description": obj.get("description"),
                "displayName": obj.get("displayName"),
                "labels": obj.get("labels"),
                "settings": (
                    SubscriptionSettings.from_dict(obj["settings"]) if obj.get("settings") is not None else None
                ),
            }
        )
        return _obj
