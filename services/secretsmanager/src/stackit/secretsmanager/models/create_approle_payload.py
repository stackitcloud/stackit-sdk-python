# coding: utf-8

"""
STACKIT Secrets Manager API

This API provides endpoints for managing the Secrets-Manager.

The version of the OpenAPI document: 1.4.3
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    StrictBool,
    StrictInt,
    StrictStr,
    field_validator,
)
from pydantic_core import to_jsonable_python
from typing_extensions import Annotated, Self


class CreateApprolePayload(BaseModel):
    """
    CreateApprolePayload
    """  # noqa: E501

    description: Optional[StrictStr] = Field(
        default="", description="A user chosen description to differentiate between multiple approles."
    )
    secret_id_num_uses: Optional[StrictInt] = 0
    secret_id_ttl: Optional[Annotated[str, Field(strict=True)]] = Field(
        default="0s", description="Token TTL in time.Duration format (eg 15m for 15 minutes, 1h for 1 hour)."
    )
    write: Optional[StrictBool] = Field(
        default=False,
        description="Is true if the approle has write access to the secrets engine. Is false for a read-only approle.",
    )
    __properties: ClassVar[List[str]] = ["description", "secret_id_num_uses", "secret_id_ttl", "write"]

    @field_validator("secret_id_ttl")
    def secret_id_ttl_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9]+[smh]$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+[smh]$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreateApprolePayload from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreateApprolePayload from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "description": obj.get("description") if obj.get("description") is not None else "",
                "secret_id_num_uses": obj.get("secret_id_num_uses") if obj.get("secret_id_num_uses") is not None else 0,
                "secret_id_ttl": obj.get("secret_id_ttl") if obj.get("secret_id_ttl") is not None else "0s",
                "write": obj.get("write") if obj.get("write") is not None else False,
            }
        )
        return _obj
