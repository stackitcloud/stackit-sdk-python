# coding: utf-8

"""
STACKIT Secrets Manager API

This API provides endpoints for managing the Secrets-Manager.

The version of the OpenAPI document: 1.4.3
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from pydantic_core import to_jsonable_python
from typing_extensions import Annotated, Self


class ApproleSecret(BaseModel):
    """
    ApproleSecret
    """  # noqa: E501

    description: StrictStr = Field(
        description="A user chosen description to differentiate between multiple approle secrets."
    )
    num_uses: StrictInt
    secret_id: Optional[UUID] = Field(
        default=None, description="Identifier used for authentication, returned only at creation"
    )
    ttl: Annotated[str, Field(strict=True)] = Field(
        description="Token TTL in time.Duration format (eg 15m for 15 minutes, 1h for 1 hour)."
    )
    version: StrictInt = Field(description="Accessor used for handling secrets")
    __properties: ClassVar[List[str]] = ["description", "num_uses", "secret_id", "ttl", "version"]

    @field_validator("ttl")
    def ttl_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9]+[smh]$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+[smh]$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ApproleSecret from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ApproleSecret from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "description": obj.get("description"),
                "num_uses": obj.get("num_uses"),
                "secret_id": obj.get("secret_id"),
                "ttl": obj.get("ttl"),
                "version": obj.get("version"),
            }
        )
        return _obj
