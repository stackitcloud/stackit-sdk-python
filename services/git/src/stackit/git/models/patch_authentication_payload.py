# coding: utf-8

"""
    STACKIT Git API

    STACKIT Git management API.

    The version of the OpenAPI document: 1beta.0.4
    Contact: git@stackit.cloud
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing_extensions import Annotated, Self


class PatchAuthenticationPayload(BaseModel):
    """
    Properties to patch on an authentication. All fields are optional.
    """  # noqa: E501

    auto_discover_url: Optional[Annotated[str, Field(strict=True)]] = Field(
        default=None, description="The well-known configuration url to use for this authentication definition."
    )
    client_id: Optional[Annotated[str, Field(strict=True, max_length=256)]] = Field(
        default=None, description="The IDP client id to use."
    )
    client_secret: Optional[StrictStr] = Field(default=None, description="The IDP client secret to use.")
    icon_url: Optional[Annotated[str, Field(strict=True)]] = Field(
        default=None, description="The url of the icon to use for this authentication definition."
    )
    provider: Optional[StrictStr] = Field(default=None, description="The Oauth2 provider to use.")
    scopes: Optional[StrictStr] = Field(default=None, description="Scopes defines the OIDC scopes to request.")
    __properties: ClassVar[List[str]] = [
        "auto_discover_url",
        "client_id",
        "client_secret",
        "icon_url",
        "provider",
        "scopes",
    ]

    @field_validator("auto_discover_url")
    def auto_discover_url_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^https:\/\/[a-zA-Z0-9\-\.]+(\.[a-zA-Z]{2,})+(\/.*)?$", value):
            raise ValueError(
                r"must validate the regular expression /^https:\/\/[a-zA-Z0-9\-\.]+(\.[a-zA-Z]{2,})+(\/.*)?$/"
            )
        return value

    @field_validator("icon_url")
    def icon_url_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^https:\/\/[a-zA-Z0-9\-\.]+(\.[a-zA-Z]{2,})+(\/.*)?$", value):
            raise ValueError(
                r"must validate the regular expression /^https:\/\/[a-zA-Z0-9\-\.]+(\.[a-zA-Z]{2,})+(\/.*)?$/"
            )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PatchAuthenticationPayload from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PatchAuthenticationPayload from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "auto_discover_url": obj.get("auto_discover_url"),
                "client_id": obj.get("client_id"),
                "client_secret": obj.get("client_secret"),
                "icon_url": obj.get("icon_url"),
                "provider": obj.get("provider"),
                "scopes": obj.get("scopes"),
            }
        )
        return _obj
