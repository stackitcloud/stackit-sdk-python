# coding: utf-8

"""
STACKIT Dremio API

This API provides endpoints for managing Dremio instances and users.

The version of the OpenAPI document: 1beta.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from pydantic_core import to_jsonable_python
from typing_extensions import Self

from stackit.dremio.models.auth_parameters import AuthParameters
from stackit.dremio.models.oauth_jwt_claims import OauthJwtClaims


class Oauth(BaseModel):
    """
    OIDC authentication configuration.
    """  # noqa: E501

    authority_url: StrictStr = Field(
        description="The Issuer location URI, where the OIDC provider configuration can be found.", alias="authorityUrl"
    )
    client_id: StrictStr = Field(description="The client ID assigned by the Identity Provider.", alias="clientId")
    client_secret: Optional[StrictStr] = Field(
        default=None, description="The client secret generated by the Identity Provider.", alias="clientSecret"
    )
    jwt_claims: OauthJwtClaims = Field(alias="jwtClaims")
    parameters: Optional[List[AuthParameters]] = Field(
        default=None, description="Any additional parameters the Identity Provider requires."
    )
    redirect_url: Optional[StrictStr] = Field(
        default=None,
        description="The URL where the Dremio instance is hosted. The URL must match the redirect URL set in the Identity Provider.",
        alias="redirectUrl",
    )
    scope: Optional[StrictStr] = Field(
        default="openid profile",
        description="A list of space-separated scopes. The `openid` scope is always required; other scopes can vary by provider.",
    )
    __properties: ClassVar[List[str]] = [
        "authorityUrl",
        "clientId",
        "clientSecret",
        "jwtClaims",
        "parameters",
        "redirectUrl",
        "scope",
    ]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Oauth from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set(
            [
                "redirect_url",
            ]
        )

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of jwt_claims
        if self.jwt_claims:
            _dict["jwtClaims"] = self.jwt_claims.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in parameters (list)
        _items = []
        if self.parameters:
            for _item_parameters in self.parameters:
                if _item_parameters:
                    _items.append(_item_parameters.to_dict())
            _dict["parameters"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Oauth from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "authorityUrl": obj.get("authorityUrl"),
                "clientId": obj.get("clientId"),
                "clientSecret": obj.get("clientSecret"),
                "jwtClaims": OauthJwtClaims.from_dict(obj["jwtClaims"]) if obj.get("jwtClaims") is not None else None,
                "parameters": (
                    [AuthParameters.from_dict(_item) for _item in obj["parameters"]]
                    if obj.get("parameters") is not None
                    else None
                ),
                "redirectUrl": obj.get("redirectUrl"),
                "scope": obj.get("scope") if obj.get("scope") is not None else "openid profile",
            }
        )
        return _obj
