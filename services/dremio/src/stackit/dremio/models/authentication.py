# coding: utf-8

"""
STACKIT Dremio API

This API provides endpoints for managing Dremio instances and users.

The version of the OpenAPI document: 1beta.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import BaseModel, ConfigDict, StrictStr, field_validator
from pydantic_core import to_jsonable_python
from typing_extensions import Self

from stackit.dremio.models.azuread import Azuread
from stackit.dremio.models.oauth import Oauth


class Authentication(BaseModel):
    """
    Dremio instance authentication settings. A change here triggers a Dremio restart and will incur downtime.
    """  # noqa: E501

    azuread: Optional[Azuread] = None
    oauth: Optional[Oauth] = None
    type: StrictStr
    __properties: ClassVar[List[str]] = ["azuread", "oauth", "type"]

    @field_validator("type")
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["local-only", "azuread", "oauth"]):
            raise ValueError("must be one of enum values ('local-only', 'azuread', 'oauth')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Authentication from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of azuread
        if self.azuread:
            _dict["azuread"] = self.azuread.to_dict()
        # override the default output from pydantic by calling `to_dict()` of oauth
        if self.oauth:
            _dict["oauth"] = self.oauth.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Authentication from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "azuread": Azuread.from_dict(obj["azuread"]) if obj.get("azuread") is not None else None,
                "oauth": Oauth.from_dict(obj["oauth"]) if obj.get("oauth") is not None else None,
                "type": obj.get("type"),
            }
        )
        return _obj
