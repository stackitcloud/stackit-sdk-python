# coding: utf-8

"""
STACKIT Container Registry API

STACKIT Container Registry management API.

The version of the OpenAPI document: 1.0.0
Contact: devex_development@stackit.cloud
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from pydantic_core import to_jsonable_python
from typing_extensions import Self

from stackit.registry.models.artifactory import Artifactory


class ArtifactoryList(BaseModel):
    """
    A list of artifactories inside a STACKIT project.
    """  # noqa: E501

    artifactories: List[Artifactory]
    next_page_token: Optional[StrictStr] = Field(
        default=None,
        description="A token to retrieve the next page of results. Pass this value as `page_token` in the subsequent request. If empty, there are no more pages.",
    )
    __properties: ClassVar[List[str]] = ["artifactories", "next_page_token"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ArtifactoryList from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in artifactories (list)
        _items = []
        if self.artifactories:
            for _item_artifactories in self.artifactories:
                if _item_artifactories:
                    _items.append(_item_artifactories.to_dict())
            _dict["artifactories"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ArtifactoryList from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "artifactories": (
                    [Artifactory.from_dict(_item) for _item in obj["artifactories"]]
                    if obj.get("artifactories") is not None
                    else None
                ),
                "next_page_token": obj.get("next_page_token"),
            }
        )
        return _obj
